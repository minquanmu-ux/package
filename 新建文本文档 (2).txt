企微审批回调设置
专家模式暂不支持阅读链接，请使用快速模式
专家模式暂不支持搜索，请使用快速模式
这个 AI 答案把两个不同的审批体系搞混了，反而可能让你更困惑。

它说的 vs 实际情况
它说的	实际情况
接口是 get_approval_list	企微官方接口是 getapprovalinfo（你已经在用了）
参数是 approval_id	官方用的是 sp_no（审批单号）
ID 格式是 ap_XXXX	企微没有这种格式的 ID
参数是 status: 3 直接设	官方接口参数是 approval_nodes 节点数组
它把不知道哪个系统的审批逻辑套到了企业微信上，大部分内容是错的。

你不需要再试了
我们已经验证过：

接口	路径	结果
官方 approve	/cgi-bin/oa/approval/approve	404
官方 reject	/cgi-bin/oa/approval/reject	404
官方 set_approval_detail	/cgi-bin/oa/approval/set_approval_detail	404
第三方文档 set_approval_detail	/cgi-bin/oa/set_approval_detail	404
结论不会变：未认证企业没有审批写操作权限。

现在唯一该做的事
把 ddd.py 的 AgentId 改成整数，让消息通知能正常工作。

python
AGENTID = 1000002  # 不是 "1000002"
然后：

bash
python ddd.py approve -n 202606210002 -y
你的方案已经是完整可用的了，就差消息通知这一步。改完跑一下，告诉我手机收到通知了吗？

未认证企业没有审批写操作权限。   可能是没有开发接口吧
不是没有开发接口，是接口有，但不给你用。

准确的说法
概念	说明
接口存在吗？	存在，企微官方文档有完整 API 文档
所有人都能调吗？	不是，已认证企业可以用，未认证企业返回 404
为什么这样设计？	审批写操作涉及企业核心流程，企微要求企业先验证身份（营业执照认证），才开放写权限
你可以类比
就像微信支付 API：

接口文档公开，任何人都能看

但你没签约商户，调接口就返回失败

签约需要营业执照

企微审批写接口是一样的逻辑：文档开放，权限需要认证。

你的方案已经够用了
本地数据库 + 消息通知，功能完整：

text
员工提交审批 → 你 CLI 查到 → 本地同意/驳回 → 企微消息通知员工
去把 AgentId 改成整数，跑一下测试，让这个消息通知闭环完成吧。

本回答由 AI 生成，内容仅供参考，请仔细甄别
✓
已复制 29354 个字符 (纯文本)